services:
  redis:
    image: redis/redis-stack-server:latest
    
  backend:
    build: .
    ports:
      - "8000:8000"
      - "7860:7860"
    env_file:
      - .env
    environment:
      - REDIS_URL=redis://redis:6379
    depends_on:
      - redis